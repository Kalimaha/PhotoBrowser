<html>
    <head>
        {{> header}}
    </head>
    <body>
        <div class="container">
            <header>
                <h1>
                    {{title}}
                </h1>
                <nav>
                    Available makers: |
                    {{#each makers}}
                        <a href="{{this.id}}.html">{{this.name}}</a> |
                    {{/each}}
                </nav>
            </header>
            <hr>
            {{> thumbnails}}
        </div>
    </body>
</html>